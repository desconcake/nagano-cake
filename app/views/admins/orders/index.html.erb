<!DOCTYPE html>
<html>
<head>
</head>

<body>
  <div class="container">
    <h2>注文一覧画面</h2>
  </div>
	<div class='row'>
    <table class='table table-hover table-inverse'>
      <thead>
        <tr>
          <th>購入日時</th>
          <th>購入者</th>
          <th>注文個数</th>
          <th>注文ステータス</th>
        </tr>
      </thead>

      <tbody>
        <% @orders.each do |order| %>
         <th><%= link_to order.created_at, admins_order_path(order.id) %></th>
         <th><%= order.customer.name %></th>
         <th><%= order.order_item.quantity %></th>
         <th><% if order.order_status == true %>
              有効
             <% else %>
              無効
             <% end %>
         </th>
         <% end %>
      </tbody>
    </table>
  </div>

<footer>
</footer>

</body>
</html>
