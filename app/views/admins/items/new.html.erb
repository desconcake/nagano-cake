<!DOCTYPE html>
<html>
<head>
</head>

<body>
  <div class="container">
    <h2>商品新規登録画面</h2>
      <%= form_for(@item) do |f| %>

        <div class = "form-group">
            <div class="field">
              <%= f.attachment_field :item_image, :fill, 100, 100, fallback: "no_image.png", size: '40x40' %>
            </div>

            <div class="field">
                <label for="user_name">商品名</label>
                <%= f.text_field :name %>
            </div>

            <div class="field">
                <label for="user_name">商品説明</label>
                <%= f.text_area :description %>
            </div>

            <div class="field">
                <label for="user_name">ジャンル</label>
                <%= f.select(:genre_id,　@genres, :id, :name) %>
            </div>

            <div class="field">
                <label for="user_name">税抜価格</label>
                <%= f.text_field :non_taxed_price.to_i %>円
            </div>

            <div class="field">
                <label for="user_name">販売ステータス</label>
                <%= f.select :sale_status,[["売り切れ",0],["販売中",1]]%>
            </div>

            <div class="field">
              <%= f.submit "変更を保存" %>
            </div>
        </div>
      <% end %>
<footer>
</footer>

</body>
</html>




