<!DOCTYPE html>
<html>
<head>
</head>

<body>
  <div class="container">
    <h2>商品新規登録画面</h2>
      <%= form_for @item, url: admins_items_path do |f| %>
        <div class = "form-group">
            <div class="field">
             <%= attachment_image_tag @item, :item_image, :fill, 400, 400, fallback: "no_image.png", size: '400x400'  %>
              <%= f.attachment_field :item_image %>
            </div>

            <div class="field">
                <label for="user_name">商品名</label>
                <%= f.text_field :name %>
            </div>

            <div class="field">
                <label for="user_name">商品説明</label>
                <%= f.text_field :description %>
            </div>

            <div class="field">
                <label for="user_name">税抜価格</label>
                <%= f.text_field :non_taxed_price %>円
            </div>

            <div class="field">
                <label for="user_name">販売ステータス</label>
                <%= f.select :sale_status,[["売り切れ",0],["販売中",1]]%>
            </div>

            <div class="field">
              <%= f.submit "新規登録" %>
            </div>
        </div>
      <% end %>
<footer>
</footer>

</body>
</html>



