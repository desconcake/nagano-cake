<!DOCTYPE html>
<html>
<head>
</head>

<body>
  <div class="container">
    <h2>商品一覧画面</h2>
  </div>
	<div class='row'>
    <table class='table table-hover table-inverse'>
      <thead>
        <tr>
          <th>注文ID</th>
          <th>商品名</th>
          <th>税抜き価格</th>
          <th>ジャンル</th>
          <th>ステータス</th>
        </tr>
      </thead>

      <tbody>
        <% @items.each do |item| %>
         <th><%= item.id %></th>
         <th><%= link_to item.name, staffs_item_path(item.id) %></th>
         <th><%= item.genre.name %></th>
         <th><%= item.sale_status.i18n %></th>
         <% end %>
      </tbody>
    </table>
  </div>

<footer>
</footer>

</body>
</html>
